# 修复任务

## IPv4 多网卡出口修复

2026-09-05 新发现：IPv4 WoL 使用通配源地址和全局广播，被本机路由选到 VirtualBox 网卡。此项独立于此前 R1-R17。

- [x] 运行时枚举有效接口，按目标网段最长前缀匹配，使用实际掩码计算定向广播；同等匹配存在歧义时拒绝发送。
- [x] 绑定源地址和接口索引；无匹配、无广播能力、接口绑定及发送失败均明确报错，不回退全局广播。
- [x] 覆盖多网卡、非 /24 掩码、边界地址、无匹配/歧义及真实枚举回归；不向真实设备发送测试唤醒包。
- [x] 格式、all-target 测试、Clippy `-D warnings`、Windows/Linux 构建；更新验证结果和使用限制。

验收：新增 8 项 IPv4 回归，生产测试共 99 项、预览测试 31 项全部通过；格式与 Clippy `-D warnings` 通过。串行清理后 Windows/Linux release 构建通过，Linux all-targets 交叉编译通过，仅出现已批准的 Zig 链接优化警告。本机真实枚举和 socket 绑定确认接口 19 的源地址为 `192.168.0.5`、广播为 `192.168.0.255/24`，测试未发送真实唤醒包。Linux systemd 模板补充枚举接口所需的 `AF_NETLINK`，保持能力集为空；旧 unit 的更新步骤见 README。Linux 原生接口枚举、systemd 运行与真实硬件唤醒仍需实机验收。

范围：2026-09-05 全项目复审的 R1-R17。只修改这些问题涉及的实现、测试、构建和文档，不重做布局或添加业务功能。

状态说明：只有修复及对应回归测试通过才勾选。代码已修改但未验证仍保持未完成。

当前检查点：R1-R17 修复完成。生产测试 91 项、独立预览测试 31 项、格式检查及 Clippy `-D warnings` 通过；Windows release 与 Linux release/all-targets 构建通过。G 盘 Zig 已更新为官方 0.16.0 稳定版，SHA-256 校验通过，系统 PATH 和构建脚本均已切换，旧开发版保留作回退。用户已明确接受 Zig 的 `ignoring deprecated linker optimization setting '1'` 警告；保持诊断可见，不得恢复全类屏蔽。未使用 WSL，原生 Linux 与硬件验收边界见下文。

## 日志与生命周期

- [x] R1 配置解析错误泄露原文。解析边界不保留原文；Display、Debug、完整 FATAL 文件和终端日志回归均通过，保留错误位置。
- [x] R2 停止服务遗漏连接线程。监听拥有会话取消与 join；真实认证连接在停止后关闭，连接计数归零，模拟唤醒次数不增加。
- [x] R8 异常日志穿透 TUI、异常退出漏收尾。作用域回收客户端/服务线程，后台不恢复终端；日志接收端消失、文件写失败、锁内 panic 延迟报告和事件通道关闭回归通过。真实终端设备故障注入未运行。

## 配置与权限

- [x] R3 热加载 ACL、握手名单和主机目标不一致。原子发布完整运行配置；真实握手、目标/目录变化、撤销关闭会话、监听变更停止及命令行覆盖保持的回归通过。
- [x] R4 保存失败后界面、内存、磁盘不一致。候选配置统一提交；替换前失败保留原授权，替换后同步失败采用已写入配置并报告持久化不确定，不回滚已授权凭据文件。两类回归及失败部署状态通过。
- [x] R5 Windows 并发读取触发误撤权。普通只读句柄并存和持有安全读取句柄时原子替换的回归通过；真正非法配置仍拒绝授权。
- [x] R14 两平台私密文件检查不一致。Windows 句柄 ACL/所有者/链接和并发读取回归通过。Linux 最终符号链接、部署权限及保存保留 owner/group/mode 的实现与测试已交叉编译；原生文件系统执行未验证。
- [x] R15 验证凭据与实际删除文件不绑定。Windows 验证同一删除句柄，竞争回归通过。Linux 隔离、验证、无覆盖恢复及恢复冲突保留原文件的实现与测试已交叉编译；原生文件系统执行未验证。

## 唤醒与连接

- [x] R6 恢复回执重置倒计时、票据提前过期。共享首回执计时；账本恢复租约不延长墓碑。真实 Noise 重连验证恢复回执后模拟 WoL 总次数仍为 1；TUI 重复回执不退回已上线状态。客户端自动恢复最多 13 分钟，过期不重提旧操作，边界测试和服务端保留期限编译期不变量已通过。
- [x] R7 部分失败显示绿色 OK。CLI/TUI 共用原始目标聚合，部分失败不会绿色 OK，拒绝按主机 ID 对齐；相应渲染与独立 CLI 进程回归通过。
- [x] R9 同步探测阻塞会话。探测任务有全局并发上限、取消与 join；慢探测期间同连接唤醒仍在回执预算内完成，回归通过。
- [x] R10 时钟容差规则分裂。在线事件采用配置时钟容差，正负边界、过期及非法未来时间回归通过。
- [x] R13 双栈域名只尝试首个地址。最多 16 个候选共享连接预算，前项失败后继续，关闭可取消；回环地址回退测试通过。

## CLI 与构建

- [x] R11 数字 4 的业务行为随入口变化。顶层页面统一启动副作用，4 个入口的真实线程启动/停止回归通过，编辑字段仍采用输入分支。
- [x] R12 非交互 CLI 失败返回 0、字面颜色占位符及重定向转义。独立进程复用实际唤醒命令和退出边界，以通道模拟 actor；全部拒绝、部分失败、断连、回执超时均退出 1，全部上线退出 0，无 ANSI 或颜色占位符。
- [x] R16 Linux 链接器诊断被屏蔽。已删除 `-A linker-messages`，更新 Zig 0.16.0 并完成 Linux release 构建。仅 `ignoring deprecated linker optimization setting '1'` 为用户批准的验收例外；其余诊断不屏蔽、不忽略。
- [x] R17 PowerShell 部署命令缺少调用运算符。生成 `&` 与单引号引用；真实 PowerShell 子进程验证空格、单引号、美元符和反引号保持字面值。

## 完成检查

- [x] 将隔离复现改为正式正确行为回归测试；原报告归档到 `docs/review-20260905.md`，隔离缺陷副本随 `cargo clean` 删除。
- [x] 检查并修正本次改动引起的旧测试失败、编译错误和警告；仅保留用户批准的 Zig 诊断例外。
- [x] `cargo fmt --all --check`。
- [x] `cargo test --all-targets --locked`，生产 91 项、独立预览 31 项通过。
- [x] `cargo clippy --all-targets --locked -- -D warnings`。
- [x] Linux release/all-targets 交叉编译，未屏蔽链接诊断；Windows release 已检查帮助、互斥参数和非 TTY 退出码/无 ANSI 输出，Linux 产物确认为 ELF64。
- [x] 更新 README/审计说明，明确热更新、恢复窗口、文件权限契约和完整测试命令；不把 M1/M4 等残余风险列为已消除。
- [x] 最终 diff 无空白错误；删除旧生成目录、隔离副本和已核实身份的失败测试临时配置，保留原审计报告。未执行原生 Linux 权限/systemd、真实终端故障注入或真实网卡 IPv6 WoL，不以交叉编译替代这些运行检查。

最终产物：`target/x86_64-pc-windows-msvc/release/RemoteOpenPower.exe` 与 `target/x86_64-unknown-linux-gnu/release/RemoteOpenPower`。Cargo metadata 已确认中间编译目录在 `E:\Catch\Cargo\Catch\build`，最终产物目录在本仓库 `target`。本轮未提交或推送 Git。

构建必须串行。Cargo/rustup/临时缓存位于 `E:\Catch\Cargo`，最终产物位于本仓库 `target`。
禁止使用 WSL（包括查询、启动和测试）；Linux 使用 G 盘 Zig 交叉编译，原生权限/systemd 测试无可用环境时明确标为未验证。
不改动真实部署配置、凭据或设备；网络测试使用回环地址和无副作用的模拟唤醒器。
